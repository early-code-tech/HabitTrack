import { View, Text, StyleSheet } from "react-native";

export default function AddHabit() {
  return (
    <View style={styles.container}>
      <Text>AddHabit Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
